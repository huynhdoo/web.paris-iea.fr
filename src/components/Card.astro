---
import Datetime from "./Datetime";

const { href, frontmatter, secHeading, collection } = Astro.props;
const { title, pubDatetime, description, image } = frontmatter;
const cover =
  "https://ik.imagekit.io/2w5ub6jhu/IEA/" + collection + "/" + image;
---

<a href={href}>
  <li
    class="box-border flex h-36 flex-row overflow-hidden rounded-b-lg border-2 border-iea-brown hover:bg-skin-card lg:h-96 lg:flex-col"
  >
    {
      image && (
        <img
          class="h-full w-1/3 shrink-0 object-cover lg:h-1/2 lg:w-full"
          src={cover}
        />
      )
    }
    <div class="p-2">
      <h2 class="text-xs font-medium uppercase lg:text-sm">{title}</h2>
      <p class="line-clamp-5 text-xs font-light lg:text-sm">{description}</p>
    </div>
  </li>
</a>
